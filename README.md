# lnx_hw_simle_project

Check that you have everything for compilation installed:
sudo apt update
sudo apt install build-essential autoconf automake

Open terminal and go to directory where you have files installed

To configurate do:
autoreconf --install
./configure
make
make install

If you want to clean everything use:
sudo make clean-all

